name: UBI IB Collector (Branch)
run-name: ${{ github.actor }} is Collecting a Branch! 🚀
on:
  push:
    branches-ignore:
      - main
  workflow_dispatch:

jobs:
  Date:
    runs-on: ubuntu-latest
    outputs:
      Date: ${{ steps.date.outputs.date }}
    steps:
      - name: Get current date
        id: DATE
        run: |
          echo "Setting Date"
          echo "DATE=$(date '+%Y%m%d-%H%M')" >> $GITHUB_OUTPUT
          echo -e "\nWrote to file:\n$GITHUB_OUTPUT\n"
          echo "Values:"
          cat $GITHUB_OUTPUT

  Skopeo:
    needs: [Date]
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Determine runner mounts
        run: |
          sudo echo -e "Disk freespace and mounts\n$(df -hT)\n\n$(mount)" | tee runner-disks.txt
      - name: Check out repository code
        uses: actions/checkout@v6

      - name: Install Skopeo
        run: |
          sudo apt-get update -y
          sudo apt-get install -y skopeo pigz

      - name: ${{ vars.REGISTRY }} Login
        run: |
          skopeo login ${{ vars.REGISTRY }}  \
            -u ${{ secrets.REGISTRY_USER }} \
            -p ${{ secrets.REGISTRY_TOKEN }}

      - name: Run Skopeo Sync
        run: |
          skopeo sync \
            --src yaml --dest dir \
            ${{ vars.COLLECTOR }}.yml \
            ${{ vars.COLLECTOR }}

      - name: Directory Contents
        run: |
          echo -e "Directory Contents\n$(ls -lAhS *)"
          echo -e "\n\nDirectory Size\n$(du -hd1)"

      - name: TARchive Skopeo Output
        run: |
          tar -cv -Ipigz --remove-files \
            -f ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz ${{ vars.COLLECTOR }} \
          | tee ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz.txt

      - name: Generate Hash
        run: |
          sha256sum -b ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz | tee \
            ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz.sha

      - name: List files 
        run: ls -1sh ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz*

      - name: Upload TARchive, ToC, and hash
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz
          path: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.tgz*
          if-no-files-found: error

  Code:
    needs: [Date]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6
        with:
          submodules: true
          lfs: true

      - name: Setup Git
        run: |
          git config user.name "$(git log -n 1 --pretty=format:%an)"
          git config user.email "$(git log -n 1 --pretty=format:%ae)"

      - name: Update Submodules if needed
        shell: bash
        run: |
          git submodule update --init --recursive --remote
          set +e
          git commit -am "Update Submodules"
          if [ $? -eq 1 ];then
            echo "No push required"
          else
            git push
          fi

      - name: Bundle Repo1 code base for ${{ vars.COLLECTOR }}
        run: |
          git bundle create --progress ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.bundle --all
          echo -e "\nDirectory Listing\n$(ls -1sht *)"

      - name: Generate Bundle Hash
        run: |
          sha256sum -b ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.bundle | tee \
            ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.bundle.sha

      - name: Upload bundle and hash
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.bundle
          path: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.bundle*
          if-no-files-found: error

      - name: TARchive code base for ${{ vars.COLLECTOR }}
        run: |
          tar -cvC .. \
            -f ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz $(basename ${{ github.WORKSPACE }}) \
            | tee ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz.txt

      - name: Generate TARchive Hash
        run: |
          sha256sum -b ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz | tee \
            ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz.sha

      - name: Directory Listing
        run: echo -e "\nDirectory Listing for $PWD\n$(ls -1Ssh)"

      - name: Upload Tarchive, ToC, and hash
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz
          path: ${{ vars.COLLECTOR }}-code-${{ needs.Date.outputs.Date }}.tgz*
          if-no-files-found: error

  Scan:
    name: Compliance scan (Trivy + Grype)
    runs-on: ubuntu-latest
    needs: [Date]
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y curl jq python3 python3-yaml skopeo

      - name: Install Trivy + Grype
        run: |
          set -euo pipefail

          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sudo sh -s -- -b /usr/local/bin

          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh \
            | sudo sh -s -- -b /usr/local/bin

      - name: Login to registry
        run: |
          set -euo pipefail

          skopeo login "${{ vars.REGISTRY }}" \
            -u "${{ secrets.REGISTRY_USER }}" \
            -p "${{ secrets.REGISTRY_TOKEN }}"

          echo "${{ secrets.REGISTRY_TOKEN }}" \
            | docker login "${{ vars.REGISTRY }}" \
                -u "${{ secrets.REGISTRY_USER }}" \
                --password-stdin

      - name: Resolve images from collector YAML
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          import re
          import subprocess
          import sys
          from pathlib import Path

          import yaml

          collector_file = Path("${{ vars.COLLECTOR }}.yml")
          data = yaml.safe_load(collector_file.read_text())

          registry = "${{ vars.REGISTRY }}"
          block = data.get(registry)
          if not isinstance(block, dict):
              raise SystemExit(f"Registry '{registry}' not found in {collector_file}")

          images_by_tag_regex = block.get("images-by-tag-regex", {})
          if not isinstance(images_by_tag_regex, dict) or not images_by_tag_regex:
              raise SystemExit(f"No images-by-tag-regex block found for {registry}")

          resolved = []

          for repo, pattern in images_by_tag_regex.items():
              regex = str(pattern).strip()
              try:
                  compiled = re.compile(regex)
              except re.error as e:
                  raise SystemExit(f"Invalid regex for {repo}: {regex} ({e})")

              full_repo = f"{registry}/{repo}"
              print(f"Resolving tags for {full_repo} with regex {regex}", file=sys.stderr)

              proc = subprocess.run(
                  ["skopeo", "list-tags", f"docker://{full_repo}"],
                  capture_output=True,
                  text=True,
                  check=False,
              )

              if proc.returncode != 0:
                  print(proc.stdout, file=sys.stderr)
                  print(proc.stderr, file=sys.stderr)
                  raise SystemExit(f"Failed to list tags for {full_repo}")

              payload = json.loads(proc.stdout)
              tags = payload.get("Tags", []) or []

              matches = sorted(tag for tag in tags if compiled.search(tag))
              if not matches:
                  raise SystemExit(f"No tags matched {regex} for {full_repo}")

              for tag in matches:
                  resolved.append(f"{full_repo}:{tag}")

          Path("images.txt").write_text("\n".join(resolved) + "\n")
          print("Resolved images:")
          for image in resolved:
              print(image)
          PY

          test -s images.txt || {
            echo "ERROR: no images resolved from ${{ vars.COLLECTOR }}.yml"
            exit 1
          }

      - name: Show resolved images
        run: cat images.txt

      - name: Trivy scan
        run: |
          set -euo pipefail
          mkdir -p scan-results/trivy
          : > scan-results/trivy-report.txt

          while read -r img; do
            [ -n "$img" ] || continue
            safe="$(printf '%s' "$img" | sed -E 's/[^A-Za-z0-9._-]+/_/g')"
            json_file="scan-results/trivy/${safe}.json"

            echo "=== Scanning $img ===" | tee -a scan-results/trivy-report.txt

            trivy image \
              --quiet \
              --scanners vuln \
              --format json \
              -o "$json_file" \
              "$img" \
              >> scan-results/trivy-report.txt 2>&1 || true

            if [ ! -s "$json_file" ]; then
              echo "FATAL: JSON output missing for $img ($json_file)" | tee -a scan-results/trivy-report.txt
              continue
            fi

            echo "=== $img ===" >> scan-results/trivy-report.txt
            trivy convert --format table "$json_file" >> scan-results/trivy-report.txt 2>&1 || true
            echo >> scan-results/trivy-report.txt
          done < images.txt

          if grep -Eqi 'Fatal error|FATAL' scan-results/trivy-report.txt; then
            echo "Compliance failed: Trivy fatal/conversion error."
            exit 1
          fi

      - name: Grype scan
        run: |
          set -euo pipefail
          mkdir -p scan-results/grype
          : > scan-results/grype-report.txt

          while read -r img; do
            [ -n "$img" ] || continue
            safe="$(printf '%s' "$img" | sed -E 's/[^A-Za-z0-9._-]+/_/g')"

            echo "=== Scanning $img ===" | tee -a scan-results/grype-report.txt
            grype "$img" -o json > "scan-results/grype/${safe}.json" \
              2>> scan-results/grype-report.txt || true
          done < images.txt

      - name: Upload scan evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}-scan
          path: scan-results/
          if-no-files-found: error

  MakeISO:
    runs-on: ubuntu-latest
    needs: [Date, Code, Skopeo, Scan]
    env:
      GH_TOKEN: ${{ github.token }}
    permissions:
      contents: write
      pull-requests: read
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6

      - name: Install mkisofs
        run: |
          sudo apt-get update -y
          sudo apt-get install -y mkisofs isomd5sum wget

      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true
          path: ${{ vars.COLLECTOR }}

      # Download Binaries if needed
      # Binaries Downloaded

      - name: Copy README to ${{ vars.COLLECTOR }} ISO
        run: cp -v README.md ${{ vars.COLLECTOR }}/

      - name: Show Artifacts
        run: ls -1sh ${{ vars.COLLECTOR }}

      - name: Generate Artifact Hash File
        run: |
          cd ${{ vars.COLLECTOR }}
          sha256sum -b *.* | tee \
            ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.sha

      - name: Upload Artifact Hash File
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.sha
          path: ${{ vars.COLLECTOR }}/${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.sha
          if-no-files-found: error

      - name: ISO Table of contents
        run: |
          tree ${{ vars.COLLECTOR }} \
            | tee ${{ vars.COLLECTOR }}/${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso.txt

      - name: Upload ISO ToC artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso.txt
          path: ${{ vars.COLLECTOR }}/${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso.txt
          if-no-files-found: error

      - name: Make ISO
        run: |
          mkisofs -J -R -v -T -l \
            -V '${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}' \
            -A '${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}' \
            -o ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso \
            ${{ vars.COLLECTOR }}

      - name: Implant MD5 sum into iso
        run: implantisomd5 ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso

      - name: Generate ISO Hash
        run: |
          sha256sum -b ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso \
          | tee ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso.sha

      - name: Show ISO & Hash
        run: ls -1sh *.iso*

      - name: Upload ISO, ToC, and hash
        uses: actions/upload-artifact@v7
        with:
          name: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso
          path: ${{ vars.COLLECTOR }}-${{ needs.Date.outputs.Date }}.iso*
          if-no-files-found: error

  Release:
    runs-on: ubuntu-latest
    needs: [Date, MakeISO]
    permissions:
      contents: write
      pull-requests: read
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6

      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Show Artifacts
        run: ls -1sh ${{ vars.COLLECTOR }}-*.*

      # Create Release

  Sharepoint:
    runs-on: ubuntu-latest
    needs: [Date, MakeISO]
    permissions:
      contents: write
      pull-requests: read
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Validate Artifacts
        run: |
          echo -e "Artifacts Downloaded\n$(find . -type f -iname *sha -execdir sha256sum -c {} \;)"

      - name: Show Artifacts
        run: ls -1sh ${{ vars.COLLECTOR }}-*.*

      # Upload files to Sharepoint
